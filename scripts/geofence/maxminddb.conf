<IfModule mod_maxminddb.c>
    MaxMindDBEnable On
    # CITY_DB omitted on purpose: unused, and GeoLite2-City.mmdb is 54MB.
    # COUNTRY_DB lives in z-geofence.conf (current DB-IP Country Lite).
</IfModule>
